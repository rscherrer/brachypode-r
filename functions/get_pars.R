# Function to generate an example parameter list
get_pars <- function() {

  # List
  alist(

    rmax <- 2,
    epsilon <- 0.1,
    K1 <- 4000,
    K2 <- 3999,
    a <- 5,
    theta1 <- 2,
    theta2 <- 5,
    c <- 0.5

  )

}
